[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn backend:backend --host 0.0.0.0 --port $PORT"
restartPolicyMaxRetries = 5
restartPolicyWindowSeconds = 600
healthchecks = {enabled = true, endpoint = "/health"}
